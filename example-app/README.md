# Example App

This is an example Capacitor app demonstrating the `@capgo/capacitor-intent-launcher` plugin.

## Setup

```bash
bun install
bun run build
npx cap add android
npx cap sync
```

## Running

```bash
npx cap open android
```

Then run the app from Android Studio on a device or emulator.
